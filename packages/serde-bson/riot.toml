[package]
name = "serde-bson"
version = "0.0.1"

[lib]
path = "src/serde_bson.ml"

[dependencies]
serde = { path = "../serde", version = "*" }
std = { path = "../std", version = "*" }

[dev-dependencies]
propane = { path = "../propane", version = "*" }
