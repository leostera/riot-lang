[package]
name = "mcp"
version = "0.0.38"
description = "Model Context Protocol transports and types"
license = "Apache-2.0"
public = true

[lib]
path = "src/mcp.ml"

[dependencies]
std = { path = "../std", version = "*" }
jsonrpc = { path = "../jsonrpc", version = "*" }
