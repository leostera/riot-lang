[package]
name = "mime"
version = "0.1.0"
description = "MIME type parsing and rendering helpers."
license = "Apache-2.0"
public = true

[lib]
path = "src/mime.ml"

[dependencies]
std = { path = "../std" }
