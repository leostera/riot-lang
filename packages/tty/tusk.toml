[package]
name = "tty"
version = "0.1.0"
description = "Terminal control and rendering helpers."
license = "Apache-2.0"
public = true

[lib]
path = "src/tty.ml"

[dependencies]
std = { path = "../std" }
