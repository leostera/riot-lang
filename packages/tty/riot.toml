[package]
name = "tty"
version = "0.0.12"
description = "Terminal control and rendering helpers"
license = "Apache-2.0"
public = true

[lib]
path = "src/tty.ml"

[dependencies]
std = { path = "../std", version = "*" }
