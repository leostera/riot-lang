[package]
name = "kernel-new"
description = "New platform abstraction layer for Riot"
version = "0.0.15"
license = "Apache-2.0"

[riot.fix]
rules = [ "-no-stdlib" ]

[lib]
path = "src/kernel_new.ml"

[dependencies]

[dev-dependencies]
std = { path = "../std", version = "*" }
propane = { path = "../propane", version = "*" }

[build-dependencies]
fixme = { path = "../fixme", version = "*" }
