[package]
name = "app"
version = "0.0.16"
description = "riot-lsp broken dependency fixture app"
license = "Apache-2.0"
public = false

[dependencies]
support = { path = "../support", version = "*" }
