[package]
name = "sqlite"
version = "0.0.1"
description = "SQLite adapter for the sqlx database APIs."
license = "Apache-2.0"

[lib]
path = "src/sqlite.ml"

[dependencies]
std = { path = "../std", version = "*" }
sqlx-driver = { path = "../sqlx-driver", version = "*" }
# Add dependencies here
