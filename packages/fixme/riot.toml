[package]
name = "fixme"
version = "0.0.1"
description = "Shared rule-authoring types for the riot-fix linting pipeline."
license = "Apache-2.0"
public = true

[lib]
path = "src/fixme.ml"

[dependencies]
std = { path = "../std", version = "*" }
syn = { path = "../syn" , version = "*"}
