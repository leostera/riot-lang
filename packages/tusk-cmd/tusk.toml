[package]
name = "tusk-cmd"
version = "0.1.0"
description = "Shared command registration interfaces for Tusk commands."
license = "Apache-2.0"
public = true

[lib]
path = "src/tusk_cmd.ml"

[dependencies]
std = { path = "../std" }
