[package]
name = "actors"
license = "Apache-2.0"
description = "Multicore actor runtime with work stealing, timers, and async syscalls"
version = "0.0.4"
categories = ["runtime", "actors"]
keywords = ["actors", "scheduler", "mailbox"]
public = true

[riot.fix]
rules = [ "-no-stdlib" ]

[riot.fix.provider]
rules = [ "loop-yield" ]

[lib]
path = "src/actors.ml"

[dependencies]
kernel = { path = "../kernel", version = "*" }

[build-dependencies]
fixme = { path = "../fixme", version = "*" }

[dev-dependencies]
propane = { path = "../propane", version = "*" }
std = { path = "../std", version = "*" }
