{
  "facts": [
    {"predicate": "edge", "args": ["a", "b"]},
    {"predicate": "edge", "args": ["b", "c"]}
  ],
  "rules": [
    {
      "head": {"predicate": "connected", "args": ["X", "Y"]},
      "body": [{"predicate": "edge", "args": ["X", "Y"]}]
    }
  ],
  "query": "connected(X, Y)",
  "result": [
    {"X": "a", "Y": "b"},
    {"X": "b", "Y": "c"}
  ]
}
