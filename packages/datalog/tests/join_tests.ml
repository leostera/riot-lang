(* Join tests - DISABLED for Phase 0: Query-Only Datalog

   These tests require rule evaluation which has been removed in Phase 0.
   Tests will be re-enabled when projection layer is added.
*)

let tests = []
