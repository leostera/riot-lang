[package]
name = "colors"
version = "0.0.1"
description = "Color science and terminal color helpers for OCaml."
license = "Apache-2.0"
public = true

[lib]
path = "src/colors.ml"

[dependencies]
std = { path = "../std" , version = "*"}
