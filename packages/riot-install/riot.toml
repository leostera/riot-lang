[package]
name = "riot-install"
version = "0.0.35"
description = "Install command orchestration on top of riot-build"
license = "Apache-2.0"
public = false

[lib]
path = "src/riot_install.ml"

[dependencies]
std = { path = "../std", version = "*" }
riot-build = { path = "../riot-build", version = "*" }
riot-deps = { path = "../riot-deps", version = "*" }
riot-model = { path = "../riot-model", version = "*" }
riot-store = { path = "../riot-store", version = "*" }
