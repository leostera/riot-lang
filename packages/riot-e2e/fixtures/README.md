# riot-e2e fixtures

This directory is reserved for static workspace fixtures used by `riot-e2e`.

Generated scenarios such as `riot init` and `riot new` should create their
workspaces during the test run instead of checking their output in here.
