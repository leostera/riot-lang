include Kernel.Int

let max_int = Kernel.Int.max_int

let min_int = Kernel.Int.min_int

let ( = ) = Kernel.( = )

let ( != ) = Kernel.( != )

let ( < ) = Kernel.( < )

let ( > ) = Kernel.( > )

let ( <= ) = Kernel.( <= )

let ( >= ) = Kernel.( >= )

let ( + ) = add

let ( - ) = sub

let ( * ) = mul

let ( / ) = div

let ( mod ) = rem
