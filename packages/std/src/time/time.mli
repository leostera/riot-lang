(** Time and duration utilities *)

module Duration = Duration
(** Duration module for representing spans of time *)

module Instant = Instant
(** Instant module for monotonic time measurements *)

module SystemTime = System_time
(** SystemTime module for wall-clock time *)
