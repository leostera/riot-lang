[package]
name = "std"
version = "0.1.0"

[tusk.fix]
rules = [ "-no-stdlib" ]

[tusk.fix.provider]
path = "fix/no_stdlib_provider.ml"
rules = [ "no-stdlib" ]

[lib]
path = "src/std.ml"

[dependencies]
miniriot = { path = "../miniriot" }
kernel = { path = "../kernel" }
