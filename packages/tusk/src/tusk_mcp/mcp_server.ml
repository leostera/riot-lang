(** MCP (Model Context Protocol) server for tusk build system *)

(** Stubbed out - will be rewritten to be miniriot async friendly *)
let start () = failwith "MCP server not yet implemented - needs async rewrite"
