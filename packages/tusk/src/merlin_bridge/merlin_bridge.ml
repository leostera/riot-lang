(** Merlin bridge - Bridge between ocaml-lsp-server and tusk build system *)

(** Stubbed out - will be rewritten to be miniriot async friendly *)
let start ~workspace:_ =
  failwith "Merlin bridge not yet implemented - needs async rewrite"
