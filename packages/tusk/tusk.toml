[package]
name = "tusk"
version = "0.0.1"

[[bin]]
name = "tusk"
path = "src/main.ml"

[dependencies]
# Base dependencies first
std = { path = "../std" }
sexp = { path = "../sexp" }
toml = { path = "../toml" }
json = { path = "../json" }
# Dependencies that depend on json
jsonrpc = { path = "../jsonrpc" }
mcp = { path = "../mcp" }
# Dependencies that depend on gluon
miniriot = { path = "../miniriot" }
# External dependencies
unix = "*"
