[package]
name = "jsonrpc"
version = "0.0.43"
description = "JSON-RPC framing, codecs, and types"
license = "Apache-2.0"
public = true

[lib]
path = "src/jsonrpc.ml"

[dependencies]
std = { path = "../std", version = "*" }
