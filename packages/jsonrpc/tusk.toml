[package]
name = "jsonrpc"
version = "0.1.0"
description = "JSON-RPC framing, codecs, and protocol types."
license = "Apache-2.0"
public = true

[lib]
path = "src/jsonrpc.ml"

[dependencies]
std = { path = "../std" }
