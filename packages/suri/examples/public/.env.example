# This is a dotfile that should be BLOCKED by the static middleware
# Accessing /public/.env.example should return 403 Forbidden

SECRET_KEY=super-secret-key-should-not-be-accessible
DATABASE_URL=postgresql://localhost/mydb
API_TOKEN=abc123xyz789

# The static middleware blocks all dotfiles by default for security
