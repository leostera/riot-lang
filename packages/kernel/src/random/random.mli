(**
   OS-backed entropy primitives for higher layers.

   `Kernel.Random` stays intentionally narrow. Pseudo-random generators,
   distributions, and sampling combinators belong in `std`. 
*)
module Source = Source
