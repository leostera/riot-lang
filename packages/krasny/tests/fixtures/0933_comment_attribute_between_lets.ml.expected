module BitMask = struct
  let lowest_set_bit_index = fun mask ->
    if mask = 0 then
      None
    else
      Some mask [@inline always]

  let remove_lowest_bit = fun mask -> mask land (mask - 1)
end
