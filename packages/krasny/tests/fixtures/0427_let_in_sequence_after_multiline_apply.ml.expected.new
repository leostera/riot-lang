let with_lock t f = f ()

let render t =
  let value = 0 in
  with_lock t
    (
      fun () ->
        ignore 1;
        ignore 2
    );
  (
    match value with
    | 0 -> ()
    | _ -> ()
  );
  ignore value

let done_ = ()
