open Std

(** Server-Sent Events parser. *)

(** SSE event type *)
type event = {
  data: string;
  event_type: string option;
  id: string option;
}

val await: Connection.t -> event Iter.MutIterator.t
