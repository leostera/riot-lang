[package]
name = "postgres"
version = "0.1.0"
description = "PostgreSQL adapter for the sqlx database APIs."
license = "Apache-2.0"
public = true

[dependencies]
std = { path = "../std" }
kernel = { path = "../kernel" }
sqlx-driver = { path = "../sqlx-driver" }
# Add dependencies here

