[package]
name = "postgres"
version = "0.0.32"
description = "PostgreSQL adapter for sqlx"
license = "Apache-2.0"
public = true

[dependencies]
std = "*"
kernel = "*"
sqlx-driver = "*"

[dev-dependencies]
dotenv = { path = "../dotenv", version = "*" }
