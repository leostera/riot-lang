# Zort's Backlog

Add future `zort` work here.

This is a date-free execution plan with work split into immediate (`now`), shortly-after (`next`), and planned-but-not-in-cycle (`later`).

Use this order: fill `now` first, then pull from `next`, then `later` only when the `next` lane is healthy.

Each item is fixture-first and includes:
- the OCaml example or `e2e-ml` case that should drive the work,
- why this is needed for zort's goals,
- what capability is currently missing,
- and what cannot be achieved yet under the current architecture.

Current focus:
- expand from the first existing-compiler program that already links against `zort` and runs on `aarch64-apple-darwin`;
- keep that bridge in the compatibility layer;
- add new `.ml` fixtures first, then implement only the `zort` capability needed to make each one link, run, trace, and benchmark;
- stage coverage from "startup and return" to allocation, GC, externals, exceptions, and then fibers/effects.

Architecture constraints for backlog items:
- Keep `zort` multicore-first. Do not add backlog items that assume a
  permanently single-domain or non-migratable runtime shape.
- Keep `zort` multi-platform. Target-specific work is allowed, but it must stay
  in the compatibility layer or host substrate and must not redefine the
  semantic kernel around one OS or ABI.
- Keep the rule `zort provides capability, userland provides policy`. Prefer
  backlog items that expose scheduler, fiber, domain, and host capabilities
  cleanly instead of hard-coding policy into the runtime.
- Keep platform gating explicit through `TargetCaps ∩ BuildCaps ∩
  RuntimePermissions`. New host-facing work should either compile out on
  unsupported targets or stay outside the semantic core.
- Prefer fixture additions that strengthen multicore effects semantics,
  migratable fibers, or cross-platform host boundaries over fixture additions
  that only deepen one target-specific compatibility corner.

## now

- [x] [P1] Add `e2e/ml/min_pure_startup.ml` and make it run against `zort`. Why: we need the smallest compiler-emitted program that does not depend on externals at all; What we have: a locked `aarch64-apple-darwin` smoke that reaches compiler-emitted startup and records the linked `caml_frametable`, `<unit>.gc_roots`, and code/data segment shapes through the compatibility harness; Cannot do yet: trust startup, backtraces, or exception unwinding for ordinary compiler-emitted code until those metadata tables are actually registered into runtime services instead of only observed.
- [ ] [P1] Add `e2e/ml/alloc_pair_zort.ml` and make it run against `zort`. Why: tuple allocation is the first real proof that compiler-emitted heap traffic agrees with `zort`; What we need: raw compiler-value support beyond tagged immediates plus the first allocation/poll ABI (`young_limit`, `caml_call_gc`, minimal `caml_alloc*` slow paths); Cannot do yet: link and run allocating compiler-emitted programs under `zort`.
- [ ] [P1] Add `e2e/ml/external_identity_zort.ml` on the normal stdlib path and make it run against `zort`. Why: the current external-only smoke avoids the stdlib entirely; What we need: broader raw-value boundary coverage and direct compiler-emitted external symbol bridging without vendor `libasmrun`; Cannot do yet: claim that ordinary compiler-emitted externals can flow through `zort` safely.
- [ ] [P1] Add `e2e/ml/mutable_pair_zort.ml` and make it run against `zort`. Why: compiled mutable writes are where GC correctness starts getting real; What we need: compiler-facing barrier and root-update entrypoints such as `caml_modify`, `caml_initialize`, and `caml_modify_generational_global_root`; Cannot do yet: trust compiled mutable code or generational root updates under GC.
- [ ] [P1] Add `e2e/ml/raise_catch_zort.ml` and `e2e/ml/raise_uncaught_zort.ml` and make them run against `zort`. Why: startup and allocation are not enough if failures cannot unwind through the compatibility layer; What we need: compiler-compatible exception raise/catch behavior, uncaught-exception reporting, and enough frame metadata to keep failure paths coherent; Cannot do yet: trust compiler-linked failures or debugging output.
- [x] [P1] Extend each zort-linked `.ml` fixture with expected output, expected trace shape, and a tiny benchmark signal. Why: the harness should drive runtime features, not just binary success; What we have: fixture-local stdout and startup-metadata trace assertions plus `bench_ns_per_run.txt` reporting for each current zort-linked case; Cannot do yet: assert richer GC/event traces until the compiler-compat shim feeds those events into the semantic runtime.
- [x] [P1] Register compiler-emitted startup metadata into `zort` compiler-compat state instead of only observing it. Why: `min_pure_startup` now proves the emitted tables are present, but they still do not participate in runtime services; What we have: compatibility-owned registrations for frame tables, raw `gc_roots`, and code/data segments plus a startup check that `caml_program` lands in a registered code fragment on the locked `aarch64-apple-darwin` path; Cannot do yet: trust backtraces, exception unwinding, or compiler-emitted allocation paths that depend on semantic root/provider ingestion.
- [ ] [P1] Teach registered compiler-compat `gc_roots` tables to expose block-field roots for `e2e/ml/alloc_pair_zort.ml`. Why: startup metadata now lands in compatibility-owned state, but allocation-driven fixtures need the registered globals to behave like collector-visible roots instead of inert table entries; What we need: raw OCaml block walking and field visitation kept inside the locked `aarch64-apple-darwin` compatibility layer; Cannot do yet: rely on compiler-emitted global blocks during allocation/GC without vendor `libasmrun`.
- [x] [P1] Expose registered compiler-compat `gc_roots` block-field slots through a host-visible API and prove it with `e2e/ml/min_global_pair_root_zort.ml`. Why: startup trace counts alone were not enough to show the registered globals expose real root edges; What we have: indexed slot lookup exported from the locked `aarch64-apple-darwin` compatibility layer plus host trace assertions that the single exposed slot is present and currently contains a block raw value; Cannot do yet: feed those exposed slots into compiler-emitted allocation/GC slow paths.
- [x] [P1] Add `e2e/ml/min_global_pair_root_zort.ml` and make its startup trace prove a compiler-emitted `gc_roots` block exposes non-zero field slots without allocator slow paths. Why: the current startup fixtures only expose zero-field module globals, so the new compatibility-owned block walker still lacks a real compiler-emitted field-bearing witness; What we have: a locked `aarch64-apple-darwin` `min_global_pair_root_zort` fixture plus the minimal no-allocation `caml_initialize` startup glue needed to finish compiler-emitted preallocated global initialization, with host trace assertions showing one registered `gc_roots` block exposing one field slot; Cannot do yet: treat those registered field slots as collector-visible semantic roots during compiler-emitted allocation/GC.
- [x] [P1] Add a `min_pure_startup` reentrant host harness that calls `caml_startup`/`caml_shutdown` twice around the same zort-linked object. Why: embedders can nest startup/shutdown, and metadata registration must not duplicate or tear down early; What we have: a zort-linked `min_pure_startup_reentrant` host harness plus reference-counted startup ownership in the locked `aarch64-apple-darwin` compatibility layer, with nested `caml_startup` calls ignored after the first initialization and metadata torn down only on the final matching `caml_shutdown`; Cannot do yet: claim the compiler-compat shim enforces the negative restart-after-shutdown fatal path under a fixture-driven host harness.
- [x] [P1] Add a `min_pure_startup_after_shutdown_fatal` host harness that expects a deterministic fatal when calling `caml_startup` after a matched `caml_shutdown`. Why: the new reentrant fixture proves nested ownership, but the compiler-facing contract also latches shutdown permanently; What we have: a zort-linked `min_pure_startup_after_shutdown_fatal` host harness plus OCaml-shaped fatal stderr/exit assertions in the locked `aarch64-apple-darwin` compatibility layer, proving that restart-after-shutdown terminates deterministically after the first startup fully tears metadata down; Cannot do yet: claim the sibling `caml_shutdown`-without-startup fatal path is end-to-end fixture-locked for embedders.
- [x] [P1] Add a `min_pure_shutdown_without_startup_fatal` host harness that expects a deterministic fatal when calling `caml_shutdown` without a matching `caml_startup`. Why: restart-after-shutdown is now fixture-locked, but the other lifecycle precondition still only had unit-test coverage; What we have: a zort-linked `min_pure_shutdown_without_startup_fatal` host harness plus OCaml-shaped fatal stderr/exit assertions in the locked `aarch64-apple-darwin` compatibility layer, proving that `caml_shutdown` without prior startup terminates deterministically before startup depth or metadata state can change; Cannot do yet: claim nested startup ownership rejects an extra post-teardown `caml_shutdown` under a fixture-backed embedding host.
- [ ] [P1] Bridge exposed compiler-compat `gc_roots` block-field slots into a root-provider-shaped collector hook for `e2e/ml/alloc_pair_zort.ml`. Why: the host-visible slot API now proves registered globals expose real root edges, but allocation/GC slow paths still cannot consume them; What we need: compatibility-owned traversal glue that can hand raw field slots to compiler-emitted allocation and collection code without leaking raw OCaml block layout into the semantic kernel; Cannot do yet: keep compiler-emitted global blocks live across `caml_call_gc` under `zort`.

## next

- [ ] [P1] Add `e2e/ml/effect_roundtrip_zort.ml` and make it run against `zort`. Why: `zort` is explicitly designed around effects and fiber control; What we need: compiler-compatible control trampolines for capture, resume, and return through the existing semantic control kernel; Cannot do yet: claim that compiler-emitted effect code can ride `zort`'s control runtime.
- [ ] [P1] Add `e2e/ml/fiber_cross_domain_zort.ml` and make it run against `zort`. Why: the runtime is being designed for multicore effects and migratable fibers; What we need: compiler-facing control integration plus deterministic scheduler/domain observations in the e2e harness; Cannot do yet: prove that compiler-emitted effect code can suspend and resume across domains under `zort`.
- [ ] [P1] Turn the `e2e-ml` harness into the main compiler-runtime acceptance harness. Why: hand-written Zig smoke cases are not enough once compiler compatibility work starts; What we need: compile/link helpers, expected output fixtures, expected trace fixtures, and microbench hooks for compiler-emitted programs; Cannot do yet: treat compiler/runtime integration as a repeatable end-to-end feature instead of manual experiments.
- [ ] [P1] Split `RuntimeServices` into platform-neutral service state plus target-selected host backends. Why: compiler compatibility should sit on a clear host substrate, especially once startup, signals, and loaders become target-specific; What we need: backend modules for POSIX, Windows, and `none`/WASI plus capability-driven import selection; Cannot do yet: clean cross-target compiler-facing backends while runtime services still own both semantic state and OS-specific ingress details.
- [ ] [P1] Compatibility GC-control façade for lifecycle operations and policy knobs. Why: compiler-emitted or user-facing compatibility code will eventually expect a GC-control surface; What we need: boundary-facing commands mapped to collector hooks and invariants; Cannot do yet: a usable subset of `Gc`-style runtime control for compiler-linked programs.
- [ ] [P1] Memory-management tuning and runtime-parameter exposure surface. Why: once compiler-linked smoke programs run, we need a stable way to observe and tune their collector behavior; What we need: stable configuration surface and diagnostics rendering; Cannot do yet: compare compiler-linked workloads meaningfully while runtime knobs remain implicit.
- [x] [P1] Add a `min_pure_startup_reentrant_extra_shutdown_fatal` host harness that expects a deterministic fatal on an extra `caml_shutdown` after a balanced nested startup/shutdown sequence. Why: the simple unmatched-shutdown path is now fixture-locked, but embedders can also over-release lifecycle ownership after a valid nested run; What we have: a zort-linked `min_pure_startup_reentrant_extra_shutdown_fatal` host harness plus OCaml-shaped fatal stderr/exit assertions in the locked `aarch64-apple-darwin` compatibility layer, proving that nested startup ownership still rejects a post-teardown extra `caml_shutdown` after the final matching shutdown clears metadata; Cannot do yet: claim the same over-release fatal path remains stable once compiler-emitted preallocated-global startup glue runs under reentrant ownership.
- [ ] [P1] Add a `min_global_pair_root_reentrant_zort` host harness that calls `caml_startup`/`caml_shutdown` twice around the same preallocated-global fixture. Why: `min_global_pair_root_zort` now proves one compiler-emitted no-allocation `caml_initialize` startup call, but embedders can re-enter startup on the same linked object; What we need: a host harness plus trace assertions that keep the locked `aarch64-apple-darwin` `c_stack` handoff in the compatibility layer while proving the no-allocation startup C-call path survives balanced nested ownership; Cannot do yet: claim compiler-emitted no-allocation startup C-call glue stays stable under reentrant embedding.
- [ ] [P1] Add a `min_global_pair_root_reentrant_extra_shutdown_fatal` host harness that expects a deterministic fatal on an extra `caml_shutdown` after a balanced nested run of `e2e/ml/min_global_pair_root_zort.ml`. Why: the pure-startup reentrant over-release path is now fixture-locked, but we still have not proved the same ownership accounting survives once compiler-emitted preallocated globals and no-allocation `caml_initialize` startup glue participate in the run; What we need: a preallocated-global reentrant host harness plus stderr/exit assertions that keep the raw `gc_roots` block layout and `c_stack` handoff in the locked `aarch64-apple-darwin` compatibility layer; Cannot do yet: claim post-teardown lifecycle over-release remains stable for the first compiler-emitted non-empty `gc_roots` startup path.

## later

- [ ] [P2] Keep growing the compiler-emitted fixture corpus in `e2e/ml/` before widening compatibility scope. Why: fixture coverage should drive what `zort` learns next instead of ad hoc runtime parity work; What we need: new `.ml` cases for strings, arrays, callbacks, finalizers, signals, and host integration as soon as each feature becomes relevant; Cannot do yet: let compatibility work stay honest without concrete executable programs.

- [ ] [P2] Typed plugin registration contract and required-metadata validation policy. Why: a typed contract enables safe optional dynlink while avoiding raw native-link assumptions; What we need: explicit metadata schema, required symbol checks, and deterministic failure shapes; Cannot do yet: complete binary loader parity until we define supported plugin formats in runtime policy.
- [ ] [P2] Platform-aware plugin loading abstraction layer. Why: decouples zort policy from host platform details; What we need: loader interface and metadata translation; Cannot do yet: parity for every native ABI variant and packaging pipeline.
- [ ] [P2] Plugin lifecycle ownership, unload, and identity continuity. Why: plugin ecosystems depend on predictable identity and release behavior; What we need: handle ownership model and unload lifecycle semantics; Cannot do yet: fully safe unload under active closures/continuations without broader graph reachability rules.
- [ ] [P2] Plugin metadata validation suites. Why: bad plugin metadata should fail fast and clearly; What we need: deterministic negative and positive test matrix; Cannot do yet: exhaustive plugin corpus until build tooling standardizes.
- [ ] [P2] Memory-profiling callback pipeline and filtering. Why: callback-based profiling enables low-overhead integration with tooling; What we need: registration lifecycle, payload schema, and filtering strategy by site/category; Cannot do yet: full low-level integration with external profilers until sink formats are standardized.
- [ ] [P2] Aggregated memory-profiling summaries by site/object class. Why: users need actionable summaries beyond raw samples; What we need: aggregation counters, export format, and summary reports; Cannot do yet: long-term stability guarantees for report schema before storage/versioning policy is set.
- [ ] [P2] Structural compare/hash baseline in compatibility layer. Why: some compatibility consumers may still require a narrow structural compare/hash surface; What we need: invalid-value matrix, traversal budgets, and traversal interruption strategy in the compatibility layer only; Cannot do yet: exhaustive parity for every custom callback mode and all historical edge-cases.
- [ ] [P2] Boundary split for runtime-hosted services. Why: keeps core semantic runtime small and maintainable; What we need: explicit module boundary for channels/sync/io-like capabilities and compatibility contract; Cannot do yet: complete host-primitive replacement until feature owners are agreed.
- [ ] [P2] Deletion-safe root-provider iteration. Why: roots can be updated during traversal in realistic workloads; What we need: tombstone/deferred-removal model with traversal invariants; Cannot do yet: full concurrent GC-thread safety model before multi-domain execution exists.
- [ ] [P2] Runtime-owned synchronization primitives with ownership and error mapping. Why: external code requires deterministic lock-like behavior without exposing raw runtime internals; What we need: first-class resource handles, lifecycle rules, and mapping to canonical exceptions; Cannot do yet: full fairness/perf tuning without dedicated sync stress harness.
- [ ] [P2] Expanded observability with regression-backed metrics artifacts. Why: changes in GC/control semantics need measurable validation; What we need: artifact schema, counters, and targeted regression tests; Cannot do yet: long-horizon dashboard conventions and governance for historical baselines.
- [ ] [P2] Generational mutation stress suite for remembered-edge correctness. Why: nursery/major boundaries are correctness-critical as code scales; What we need: mutation-heavy directed tests and stronger edge coverage; Cannot do yet: full production workload corpus for confidence before next collector milestones.
- [ ] [P2] Hardened external-result contracts and API boundary typing. Why: boundary consumers need stable semantics instead of internal heap encoding leakage; What we need: explicit result/error contract and migration away from legacy encoded assumptions; Cannot do yet: complete end-to-end migration of every external consumer path.
- [ ] [P2] Managed-stack vs system-stack overflow strategy and recovery model. Why: failure behavior must be explicit to avoid silent runaway failures; What we need: policy for stack growth/fault handling and clear ownership boundary; Cannot do yet: platform-complete stack policy until non-Unix/native backends are completed.
- [ ] [P2] Shared-memory-safe atomic mutation primitives for fields. Why: enables future parallel execution and robust low-level concurrency APIs; What we need: atomic operations with explicit ordering and API design; Cannot do yet: full integration with multiple execution domains.
- [ ] [P2] Marshaling/code-loading capability model. Why: avoids accidental compatibility traps while keeping clear limits; What we need: policy for accepted/forbidden serialization and continuation-related restrictions; Cannot do yet: exact byte-level parity with legacy compiler artifacts.
- [ ] [P2] Multi-domain GC hardening and forwarding observability plan. Why: prevents regressions when moving from baseline collector policy; What we need: explicit strategy and telemetry for forwarding/shared objects; Cannot do yet: full production-level GC concurrency without stable domain scheduler.
- [ ] [P2] Domain startup/shutdown parity foundations. Why: startup/shutdown ordering is required before reliable lifecycle APIs; What we need: reference-counted lifecycle sequencing and hook points; Cannot do yet: external host orchestration contracts for all embedding modes.
- [ ] [P2] Deterministic runtime shutdown orchestration for multi-context execution. Why: prevents teardown races and leaked work when contexts leave runtime participation; What we need: teardown sequencing model, hook ordering, and idempotency; Cannot do yet: full host coordination for forced termination and external shutdown races.
- [ ] [P2] STW progress handoff for blocking behavior. Why: blocked regions must not stall global coordination; What we need: handoff protocol and handback policy for collection phases; Cannot do yet: complete scheduler-level handoff without domain runtime.
- [ ] [P2] Aggregated global statistics model for multiple runtime participants. Why: observability must scale when runtime participants increase; What we need: aggregation strategy with approximate yet stable reporting; Cannot do yet: exactness guarantees under active migration/adoption.
- [ ] [P2] Cross-heap forwarding representation. Why: preserves sharing behavior across advanced collectors; What we need: forwarding metadata shape and stabilization semantics; Cannot do yet: full correctness proofs without dual-heap collector completion.
- [ ] [P2] Forwarding completion and convergence behavior. Why: stale or partial forwarding states are a major correctness risk; What we need: completion protocol and retry/recheck behavior; Cannot do yet: high-concurrency validation at scale.
- [ ] [P2] Orphan-safe finalizer transfer model. Why: finalizers must remain reliable under context loss/migration; What we need: ownership handoff rules and readiness queue ownership; Cannot do yet: multi-domain testbed that reproduces orphaning at scale.
- [ ] [P2] Portable signal-number mapping strategy. Why: signal APIs must remain consistent across Unix/other targets; What we need: canonical mapping table and unavailable-signal policy; Cannot do yet: full non-Unix mapping parity.
- [ ] [P2] Non-Unix signal-stack behavior policy. Why: avoids undefined behavior when features are missing; What we need: explicit capability list and documented fallbacks; Cannot do yet: equivalent native alternatives on all targets.
- [ ] [P2] Exception-aware resource unwind policy. Why: deterministic resource cleanup under exceptional exits is required for reliability; What we need: unwinding contracts for owned resources; Cannot do yet: full integration with all external resource kinds.
- [ ] [P2] Channel-like resource lifecycle governance. Why: deterministic close/error behavior is part of practical runtime semantics; What we need: lifecycle transitions and locked-state cleanup rules; Cannot do yet: complete suite of resource types and failure injections.
- [ ] [P2] Syscall bridge normalization behaviors. Why: host interactions must produce predictable, typed runtime exceptions; What we need: bridge contract for interrupted/bad-path/system-error cases; Cannot do yet: complete coverage of platform-specific syscall errors.
- [ ] [P2] Comparison/hash policy flags and compatibility boundaries. Why: maintainers need controlled behavior without overfitting to legacy internals; What we need: feature flags and documented policy matrix; Cannot do yet: community/embedding agreement on defaults.
- [ ] [P2] Byte/float array mutation + copy semantics. Why: memory semantics become correctness-critical as numeric code grows; What we need: explicit ordering, bounds, and consistency policy; Cannot do yet: final memory model guarantee for every optimization path.
- [ ] [P2] Weak/ephemeron phase parity extension. Why: advanced memory-liveness behavior depends on phase-aware cleaning; What we need: phase model and dead-key/data cleanup semantics; Cannot do yet: full scale stress coverage for complex graphs.
- [ ] [P2] Full GC phase observability model and baselines. Why: phase visibility is needed to reason about long-running collections; What we need: stable phase naming/events and baseline assertions; Cannot do yet: all collectors emitting uniform phase timelines.
- [ ] [P2] Major-collection scheduling and urgent-collection hooks. Why: memory pressure control needs explicit levers; What we need: scheduling API and urgent-work triggers; Cannot do yet: validated policy across diverse workloads.
- [ ] [P2] Runtime diagnostics and verbosity surface. Why: runtime users need operational visibility without core changes; What we need: rendering and parse/emit policy for diagnostic modes; Cannot do yet: complete legacy-field equivalence.
- [ ] [P2] Deletion-aware root-provider traversal invariants. Why: real systems mutate roots during traversal; What we need: deferred-removal pass and traversal invariants; Cannot do yet: strict lock-free concurrent GC path support.
- [ ] [P2] Callback-path parity tests for continuation backtraces and parent chains. Why: backtrace fidelity drives debugging and effect diagnosis; What we need: controlled tests for captured stack and callback boundaries; Cannot do yet: platform-specific callback metadata parity.
- [ ] [P2] Effect-boundary re-perform scenarios under callback/finalizer invocation. Why: ensures control transfer behavior stays coherent under mixed callback semantics; What we need: test matrix for boundary traversal and unhandled-effect behavior; Cannot do yet: full native-stack implementation for every edge case.
