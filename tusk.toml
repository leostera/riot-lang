[workspace]
members = ["packages/std", "packages/kernel", "packages/jsonrpc", "packages/mcp", "packages/miniriot", "packages/minitusk", "packages/tusk"]

[workspace.config]
ocaml_version = "5.3.0"
profile = "debug"

[workspace.dependencies]
# Shared external dependencies across workspace members
# Internal workspace members should be declared via path in individual package TOMLs
