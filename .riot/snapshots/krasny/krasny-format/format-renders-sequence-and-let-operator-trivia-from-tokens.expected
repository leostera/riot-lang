let run = fun () ->
  first;
  (* keep after first *)
  (* keep before second *)
  second;
  (** keep before third *)
  third

let bind =
  let* value =
    (* keep before bound value *)
    compute
  in
  (* keep before body *)
  finish value
