class 'a service:object
  val mutable state: int
  method private run: int
end = object (self)
  val mutable state = 0
  method private run =
    self#state
      (** keep body doc *)
      [%%foo]
end
