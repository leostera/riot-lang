[workspace]
members = [
  "native/hello-rust",
  "native/riot-bindgen",
  "native/riot-core",
  "native/riot-derive",
  "native/riot-ffi",
]

# Use a separate target directory to avoid conflicts with Riot's target/ directory
[workspace.metadata]
target-dir = "native/target" 
