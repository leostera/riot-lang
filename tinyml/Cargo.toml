[package]
name = "tinyml"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
logos = "0.16"
miette = { version = "7", features = ["fancy"] }
once_cell = "1"
thiserror = "2"

[dev-dependencies]
insta = "1"
