---
import { Button } from "@/components/ui/button";

interface Props {
  command: string;
}

const { command } = Astro.props;
---

<div class="copy-strip">
  <code>{command}</code>
  <Button type="button" variant="ghost" size="sm" className="copy-strip__button">
    copy
  </Button>
</div>
