{
  "name": "docs.riot.ml",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "sync:rfds": "bun scripts/sync-rfds.mjs",
    "build": "bun run sync:rfds && astro build",
    "preview": "astro preview",
    "check": "bun run sync:rfds && astro check && tsc --noEmit",
    "deploy": "bun run build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.38.2",
    "astro": "^6.0.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@types/bun": "latest",
    "typescript": "^5",
    "wrangler": "^4.87.0"
  }
}
