{
  "name": "docs.riot.ml",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun x --bun mint dev",
    "start": "bun x --bun mint dev",
    "sync:rfds": "node ./scripts/sync-rfds.mjs",
    "deploy": "bun run sync:rfds && wrangler deploy"
  }
}
