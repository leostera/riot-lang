---
title: "riot logout"
description: "Remove the saved registry session when you want local state to stop carrying credentials."
---

`riot logout` is the small but important counterpart to `riot login`.

Use it when:

- you are rotating machines
- you are cleaning up local credentials
- you want to verify that a workflow does not accidentally depend on an existing authenticated session

Good package tooling should make authentication explicit in both directions.
