\* Add statements after this line.
SPECIFICATION Spec

CONSTANTS
  BundlePresent <- SmokeBundlePresent
  BundleVersion <- SmokeBundleVersion
  ExpectedVersion <- SmokeExpectedVersion
  PackageMatches <- SmokePackageMatches
  DecodeRaises <- SmokeDecodeRaises
  ModuleGraphParses <- SmokeModuleGraphParses
  ActionGraphParses <- SmokeActionGraphParses

INVARIANTS
  TypeOK
  FreshBundleHitsCache
  CacheHitOnlyWhenBundleAccepted
  StaleVersionForcesRebuild
