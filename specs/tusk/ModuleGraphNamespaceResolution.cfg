\* Add statements after this line.
SPECIFICATION Spec

CONSTANTS
  Nodes <- SmokeNodes
  SimpleRefs <- SmokeSimpleRefs
  QualifiedRefs <- SmokeQualifiedRefs
  QualifiedNameOf <- SmokeQualifiedNameOf
  QualifiedRefOf <- SmokeQualifiedRefOf
  Registry <- SmokeRegistry
  SourceNode <- SmokeSourceNode
  SourceDeps <- SmokeSourceDeps

INVARIANTS
  TypeOK
  NoSelfEdges
  EdgesOnlyUseSimpleNameRegistry
  ResolvedQualifiedRefsMatchProcessedDependencies
